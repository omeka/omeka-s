user     = ${OMEKA__DATABASE_USER}
password = ${OMEKA__DATABASE_PASSWORD}
dbname   = ${OMEKA__DATABASE_NAME}
host     = ${OMEKA__DATABASE_HOST}
port     = ${OMEKA__DATABASE_PORT}
log_path = ${OMEKA__LOG_PATH}
