<?php
$translate = $this->plugin('translate');
$escape = $this->plugin('escapeHtml');
$this->dataType()->prepareForm();
?>

<div class="resource-property field template" data-template-id="" role="group">
    <div class="field-meta">
        <div class="field-label"></div>
        <a href="#" class="collapse" aria-label="<?php echo $escape($translate('Collapse')); ?>"></a>
        <div class="collapsible">
            <div class="field-description"></div>
            <div class="field-term" title="<?php echo $escape($translate('Property term for development use')); ?>"></div>
        </div>
    </div>
    <div class="inputs">
        <div class="values">
            <p class="no-values"><?php echo $translate('Select the first value type.'); ?></p>
        </div>
        <div class="add-values default-selector">
            <label class="data-type-label">
                <span class="data-type-label-content"><?php echo $translate('Data type'); ?></span>
                <select>
                    <option value="literal"><?php echo $translate('Text'); ?></option>
                    <option value="resource"><?php echo $translate('Omeka resource'); ?></option>
                    <option value="uri"><?php echo $translate('URI'); ?></option>
                </select>
            </label>
            <button type="button" class="o-icon-add add-value"><?php echo $translate('Add value'); ?></button>
        </div>
        <div class="add-values multiple-selector" style="display:none">
            <label class="data-type-label">
                <span class="data-type-label-content"><?php echo $translate('Data type'); ?></span>
            </label>
            <button type="button" class="o-icon-add add-value"><?php echo $translate('Add value'); ?></button>
        </div>
        <div class="add-values single-selector" style="display:none">
            <button type="button" class="o-icon-add add-value"><?php echo $translate('Add value'); ?></button>
        </div>
    </div>
</div>

<?php echo $this->dataType()->getTemplates(); ?>
