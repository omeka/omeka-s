<?php $this->htmlElement('body')->appendAttribute('class', 'items browse'); ?>
<h1>Items</h1>

<div id="page-actions">
    <a href="<?php echo $this->escapeHtmlAttr($this->url(null, array('action' => 'add'), true)); ?>" class="button">Add new item</a>
</div>

<?php echo $this->pagination(); ?>

<a href="batch-edit" class="batch-edit button"><?php echo $this->translate('Batch edit selected'); ?></a>

<table id="items-list">
    <thead>
        <tr>
            <th><input type="checkbox" id="select-all"><label class="screen-reader-text" for="select-all">Select all</label></th>
            <th><?php echo $this->sortLink('Title', 'dcterms:title'); ?></th>
            <th><?php echo $this->sortLink('Class', 'resource_class_label'); ?></th>
            <th><?php echo $this->sortLink('Date Created', 'created'); ?></th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($items as $item): ?>
        <tr>
            <td><input type="checkbox" id="row-<?php echo $item->id(); ?>"><label class="screen-reader-text" for="row-<?php echo $item->id(); ?>">Select row</label></td>
            <td>
                <span class="record-name">
                    <a href="<?php echo $this->escapeHtmlAttr($this->url('admin/id', array('action' => 'show', 'id' => $item->id()), true)); ?>"><?php echo $this->escapeHtml($item->displayTitle('[untitled]')); ?></a>
                </span>
                <ul class="actions">
                    <li><a href="<?php echo $this->escapeHtmlAttr($this->url('admin/id', array('action' => 'edit', 'id' => $item->id()), true)); ?>" class="o-icon-edit" aria-label="Edit" title="Edit"></a></li>
                    <li><a href="#" class="o-icon-more" aria-label="Details" title="Details"></a></li>
                    <li><a href="#" class="o-icon-delete" aria-label="Delete" title="Delete"></a></li>
                </ul>
            </td>
            <td><?php echo $this->escapeHtml($item->displayResourceClassLabel()); ?></td>
            <td><?php echo $this->escapeHtml($this->i18n()->dateFormat($item->created())); ?></td>
        </tr>
        <?php endforeach; ?>
    </tbody>
</table>
<div id="more" class="sidebar">
    <a href="#" class="o-icon-close" aria-label="Close details panel" title="Close"></a>
    <h2>Rocket Row</h2>
    <h3>Description</h3>
    <p>Cras mattis consectetur purus sit amet fermentum. Integer posuere erat a ante venenatis dapibus posuere velit aliquet. Vivamus sagitti
    <h3>Creator</h3>
    <p>admin</p>
    <h3>Coverage</h3>
    <p>1800-1875</p>
    <h3>Subject</h3>
    <p>Place</p>
</div>
<div id="delete" class="sidebar">
    <a href="#" class="o-icon-close" aria-label="Close details panel" title="Close"></a>
    <h2>Delete record</h2>
    <p>Are you sure you would like to delete this record?</p>
    <button>Throw it in the garbage</button>
</div>


<div class="overlay"></div>

<div class="modal">
    <div class="modal-header">
        <a href="#" class="closeBtn o-icon-close" aria-label="Close me"></a>
        <h1></h1>
    </div>
    <div class="modal-content">
    </div>
</div>
