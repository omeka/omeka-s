<?php
$this->htmlElement('body')->appendAttribute('class', 'items show');
$escape = $this->plugin('escapeHtml');
$escapeAttr = $this->plugin('escapeHtmlAttr');
?>
<h1><?php echo $escape('Item'); ?>: &ldquo;<?php echo $escape(strip_tags($item->displayTitle('[untitled]'))); ?>&rdquo;</h1>
<div id="page-actions">
    <?php echo $item->link($this->translate('Edit item'), 'edit', array('class' => 'button')); ?>
</div>

<nav class="section-nav">
    <ul>
        <li><a href="#" class="item-metadata active section"><?php echo $escape('Metadata'); ?></a></li>
        <li><a href="#" class="item-media section"><?php echo $escape('Media'); ?></a></li>
        <li><a href="#" class="item-linked section"><?php echo $escape('Linked Resources'); ?></a></li>
    </ul>
</nav>

<div id="item-metadata" class="active section">
    <?php echo $item->displayValues(); ?>
</div>

<div id="item-media" class="section">
    <?php foreach ($item->media() as $media): ?>
    <div><?php echo $media->render(); ?></div>
    <?php endforeach; ?>
</div>

<div id="item-linked" class="section">
    <?php echo $item->displayLinkedResources(); ?>
</div>

<?php $this->trigger('view.show.after'); ?>
