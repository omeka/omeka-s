<?php
$prevText = $this->translate('Prev');
$nextText = $this->translate('Next');
?>
<div class="site-page-pagination">
    <?php echo $prevPage ? $this->hyperlink($prevText, $prevPage->siteUrl()) : $prevText; ?> |
    <?php echo $nextPage ? $this->hyperlink($nextText, $nextPage->siteUrl()) : $nextText; ?>
</div>
