<h1>Change Password for &ldquo;<?php echo $user->username(); ?>&rdquo;</h1>

<nav class="section-nav">
    <?php echo $this->navigation('Omeka\Navigation\User')->menu(); ?>
</nav>

<?php
$form->prepare();
$formLabel = $this->plugin('formLabel');
$formInput = $this->plugin('formInput');
$formErrors = $this->plugin('formElementErrors');
?>

<form  method="post">
<?php foreach ($form->getElements() as $element): ?>
<div class="field">
    <div class="field-meta">
        <?php echo $formLabel($element); ?>
    </div>
    <div class="inputs">
        <?php echo $formInput($element); ?>
    </div>
    <?php echo $formErrors($element); ?>
</div>
<?php endforeach; ?>
<button>Submit</button>
</form>
