<?php
$queryArray = $this->params()->fromQuery();
if ($queryArray) {
    $searchFilters = $this->searchFilters(null, $queryArray);
    if (!$searchFilters) {
        $searchFilters = $this->translate('[Edit query below]');
    }
} else {
    $searchFilters = $this->translate('[No query. Edit query below]');
}
?>
<?php echo $searchFilters; ?>
