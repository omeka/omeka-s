<?php $this->htmlElement('body')->appendAttribute('class', 'items browse'); ?>
<h1>Items</h1>

<div id="page-actions">
    <a href="<?php echo $this->escapeHtmlAttr($this->url(null, array('action' => 'add'), true)); ?>" class="button">Add new item</a>
</div>

<?php echo $this->pagination(); ?>

<a href="batch-edit" class="batch-edit button">Batch edit selected</a>

<table id="items-list">
    <thead>
        <tr>
            <th><input type="checkbox" id="select-all"><label class="screen-reader-text" for="select-all">Select all</label></th>
            <th><?php echo $this->sortLink('Title', 'dcterms:title'); ?></th>
            <th><?php echo $this->sortLink('Class', 'resource_class_label'); ?></th>
            <th><?php echo $this->sortLink('Date Created', 'created'); ?></th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($items as $item): ?>
        <tr>
            <td><input type="checkbox" id="row-1"><label class="screen-reader-text" for="row-1">Select row</label></td>
            <td>
                <span class="record-name">
                    <a href="<?php echo $this->escapeHtmlAttr($this->url('admin/id', array('action' => 'show', 'id' => $item->getId()), true)); ?>"><?php echo $this->escapeHtml($item->getDisplayTitle('[untitled]')); ?></a>
                </span>
                <ul class="actions">
                    <li><a href="<?php echo $this->escapeHtmlAttr($this->url('admin/id', array('action' => 'edit', 'id' => $item->getId()), true)); ?>" class="o-icon-edit"><span class="screen-reader-text">Edit</span></a></li>
                    <li><a href="#" class="o-icon-more"><span class="screen-reader-text">Details</span></a></li>
                    <li><a href="#" class="o-icon-delete"><span class="screen-reader-text">Delete</span></a></li>
                </ul>
            </td>
            <td><?php echo $this->escapeHtml($item->getDisplayResourceClassLabel()); ?></td>
            <td><?php echo $this->escapeHtml($item->getCreated()->format('M j, Y')); ?></td>
        </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<div class="overlay"></div>

<div class="modal">
    <div class="modal-header">
        <a href="#" class="closeBtn o-icon-close"><span class="screen-reader-text">Close Me</span></a>
        <h1></h1>
    </div>
    <div class="modal-content">
    </div>
</div>
