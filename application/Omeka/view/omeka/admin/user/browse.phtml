<?php
$this->htmlElement('body')->appendAttribute('class', 'users browse');
$escape = $this->plugin('escapeHtml');
$attrEscape = $this->plugin('escapeHtmlAttr');
$url = $this->plugin('url');
?>
<h1>Users</h1>

<div id="page-actions">
<a href="#" class="button">Add new user</a>
</div>

<table id="items-list">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Date Added</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach($users as $user): ?>
        <?php
        $showUrl = $attrEscape($url('admin/id', array(
            'controller' => 'user',
            'id' => $user->id()
        )));
        $editUrl = $attrEscape($url('admin/id', array(
            'controller' => 'user',
            'action' => 'edit',
            'id' => $user->id()
        )));
        ?>
        <tr>
            <td></td>
            <td>
                <span class="record-name">
                    <a href="<?php echo $showUrl; ?>">
                        <?php echo $escape($user->username());?>
                        (<?php echo $escape($user->name()); ?>)
                    </a>
                </span>
                <ul class="actions">
                    <li><a href="<?php echo $editUrl; ?>" class="o-icon-edit"><span class="screen-reader-text">Edit</span></a></li>
                    <li><a href="#" class="o-icon-more"><span class="screen-reader-text">Details</span></a></li>
                </ul>
            </td>
            <td><?php echo $user->created()->format('M j, Y');?></td>
        </tr>
        <?php endforeach; ?>
    </tbody>
</table>
<pre><?php if (isset($errors)) {print_r($errors);} ?></pre>
