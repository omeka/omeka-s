<?php
$escape = $this->plugin('escapeHtml');
$escapeAttr = $this->plugin('escapeHtmlAttr');
?>

<h1>Select Item Set</h1>
<div class="search-nav">
    <div id="sidebar-resource-search" class="resource-search">
        <input type="text" value="<?php echo $searchValue; ?>" id="resource-list-search">
        <button 
        data-search-url="<?php echo $escapeAttr($this->url('admin/default', array('controller' => 'item-set', 'action' => 'sidebar-select'))); ?>" 
        type="submit" class="o-icon-search">Search</button>
    </div>
<?php echo $this->pagination(); ?>
</div>

<div class="resource-list">
    <?php foreach ($itemSets as $itemSet):?>
    <div class="resource item-set">
        <div class="placeholder"></div>
        <?php if ($media = $itemSet->primaryMedia()): ?>
        <img src="<?php echo $escapeAttr($media->thumbnailUrl('square')); ?>">
        <?php endif; ?>
        <span class="resource-name">
            <a href="#"
            class="sidebar-content"
            data-resource-id="<?php echo $itemSet->id(); ?>"
            data-sidebar-content-url="<?php echo $escapeAttr($itemSet->url('show-details')); ?>"><?php echo $escape($itemSet->displayTitle()); ?></a></span>
    </div>
    <?php endforeach; ?>
</div>


