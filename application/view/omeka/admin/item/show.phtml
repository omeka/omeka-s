<?php
$this->htmlElement('body')->appendAttribute('class', 'items show sidebar-open');
$escape = $this->plugin('escapeHtml');
$escapeAttr = $this->plugin('escapeHtmlAttr');
?>
<h1><?php echo $escape('Item'); ?>: &ldquo;<?php echo $escape(strip_tags($item->displayTitle('[untitled]'))); ?>&rdquo;</h1>
<div id="page-actions">
    <?php echo $item->link($this->translate('Edit item'), 'edit', array('class' => 'button')); ?>
</div>

<nav class="section-nav">
    <ul>
        <li class="active"><a href="#item-metadata"><?php echo $escape('Metadata'); ?></a></li>
        <li><a href="#item-linked"><?php echo $escape('Linked Resources'); ?></a></li>
    </ul>
</nav>

<div id="item-metadata" class="active section sidebar-open">
    <?php echo $item->displayValues(); ?>
</div>

<div id="item-linked" class="section">
    <?php echo $item->displayLinkedResources(); ?>
</div>


<div class="sidebar active">
    <div class="media">
        <?php foreach ($item->media() as $media): ?>
        <a href="<?php echo $media->url(); ?>"><img src="<?php echo $media->thumbnailUrl('square'); ?>"></a>
        <?php endforeach; ?>
    </div>
    <?php $itemsets = $item->itemSets(); ?>
    <?php if (count($itemsets) > 0): ?>
    <div class="property">
        <h4>Item Sets</h4>
        <?php foreach ($itemsets as $itemset): ?>
        <div class="value item-sets"><?php echo $itemset->link($itemset->displayTitle()); ?></div>
        <?php endforeach; ?>
    </div>
    <?php endif; ?>
    <div class="property">
        <h4>Date Created</h4>
        <div class="value"><?php echo $escape($this->i18n()->dateFormat($item->created())); ?></div>
    </div>
</div>
<?php $this->trigger('view.show.after'); ?>
